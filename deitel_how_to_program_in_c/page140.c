#include <stdio.h>

int main(void) {
    int count = 10;
    
    // Counter controlled repetition: loop from 1 to 10
    for (int i = 1; i <= count; i++) {
        printf("Counter: %d\n", i);
    }
    
    return 0;
}